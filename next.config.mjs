/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
  basePath: '/final-aos',
  assetPrefix: '/final-aos',
}

export default nextConfig
